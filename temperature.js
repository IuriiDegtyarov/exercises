function convertCToF(celsius) {
  let cToF = (9 * celsius + (32 * 5)) / 5;
  return (cToF);
}
function convertFToC (fahrenheit) {
  let fToC = (5 * (fahrenheit -32)) / 9;
  return (fToC);
}
convertCToF(56);
convertFToC(132);
